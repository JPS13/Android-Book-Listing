<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Style for list item text view -->
    <style name="list_item_text_view_style">
        <item name="android:layout_marginRight">@dimen/list_item_horizontal_margin</item>
        <item name="android:layout_marginLeft">@dimen/list_item_horizontal_margin</item>
        <item name="android:layout_marginTop">@dimen/list_item_vertical_margin</item>
        <item name="android:layout_marginBottom">@dimen/list_item_vertical_margin</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:maxLines">2</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Style for navigation button -->
    <style name="internet_icon_style">
        <item name="android:layout_height">@dimen/internet_icon_size</item>
        <item name="android:layout_width">@dimen/internet_icon_size</item>
        <item name="android:layout_marginLeft">@dimen/list_item_horizontal_margin</item>
        <item name="android:layout_marginRight">@dimen/activity_vertical_margin</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
